<?php

function mr_extends_navigation($nav) {
    // notify('here');
    // var_dump($nav->get('myprofile'));
    // print_object($nav);
    // $usernode = $nav->rootnodes['myprofile'];
    // $nav->get('myprofile')->add('Testing', new moodle_url('/user/profile.php'), navigation_node::TYPE_CUSTOM, 'short', 0);
}